par_NAIS.b = zeros(T,N); 
par_NAIS.C = zeros(T,N); 


lng_y = zeros(N,1);
lnp = zeros(N,1);
lng = zeros(N,1);
x_sim = zeros(T,N);
v = zeros(T,N);
F_inv = zeros(T,N);
K = zeros(T,N);
L = zeros(T,N);
eps_smooth = zeros(T,N);
